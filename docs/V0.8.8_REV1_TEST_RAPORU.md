# v0.8.8 rev1 Test Raporu

## Düzeltmeler

- Kariyer widget testi `career-scroll-view` kaydırma alanını kullanarak
  `career-back-button` düğmesini görünür hâle getirir.
- Başlangıç Sekizlisi ekranında alt **KAYDET** eylemi kiti kaydeder ve başarılı
  kayıt sonrasında açıldığı savaş hazırlık ekranına döner.
- Kit düzenleme ekranında yinelenen iç kayıt düğmesi `kitOnly` kullanımında
  gösterilmez.
- Flutter istemci yapı numarası `0.8.8+58` oldu.

## Bu ortamda çalıştırılan kontroller

- İstemci kaynak sözleşmesi: **37/37 geçti**.
- API sürücüsü gerektirmeyen normal Python paketi: **108/108 geçti**.
- Denge paketi: **6/6 geçti**.
- Python kaynak derleme kontrolü: geçti.
- YAML doğrulaması: geçti.
- Dart kaynak delimiter/yapısal kontrolü: geçti.

## Ortam sınırlaması

Bu paketleme ortamında Flutter/Dart SDK bulunmadığı için `flutter analyze` ve
`flutter test` çalıştırılamadı. Ayrıca `psycopg` sürücüsü bulunmadığından
PostgreSQL kullanan API testleri bu ortamda toplanamadı. Windows kabul komutu:

```powershell
cd D:\Projects\project-relay\client
.\tool\bootstrap_client.ps1
```
