# Project Relay v0.8.20 rev1 Test Raporu

Bu hotfix Flutter analyzer tarafından bildirilen beş bulguyu kapatır:

- async gap sonrası `sheetContext` mounted koruması,
- `BoardController` içinde `placements` değişkeninin declaration öncesi kullanımı,
- yinelenmiş `segmentedButtonTheme`,
- yinelenmiş `chipTheme`,
- gereksiz `__` wildcard parametresi.

Ek olarak `pyproject.toml` paket sürümü API ile `0.8.20` olarak eşitlendi.

Paketleme ortamında geçen kontroller:

- `tests/test_client_contract.py`: 48/48
- engine/career/collection/competitive/progression/season/social/chat/auth/alpha: 76/76
- migrations: 2/2
- hızlı balance seti: 5/5
- Python compileall: başarılı
- analyzer hata kalıplarına özel statik regresyon: başarılı

Gerçek `flutter analyze` ve `flutter test` son kabulü Windows Flutter SDK ortamında çalıştırılmalıdır.
