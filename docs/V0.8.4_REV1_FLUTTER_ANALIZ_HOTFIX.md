# Project Relay v0.8.4 rev1 — Flutter Analiz Hotfix'i

**API:** `0.8.4`  
**Flutter istemci:** `0.8.4+49`  
**Savaş kuralları:** `0.8`

## Yerel analizde bildirilen bulgular

- `career_screen.dart`: `_SectionCard.trailing` parametresine hiçbir çağrıda değer verilmemesi
- `social_screen.dart`: `_MetricPill` bileşeninin kullanılmaması
- `social_screen.dart`: `_MetricPill.accent` parametresine değer verilmemesi

## Uygulanan düzeltme

Kullanılmayan parametre ve bileşenler kaldırıldı. Herhangi bir ekran akışı, API
sözleşmesi, veritabanı şeması veya savaş davranışı değiştirilmedi.

## Paketleme doğrulaması

- Normal Python/API/sözleşme testleri: **140/140 geçti**
- Seçili denge regresyonları: **5/5 geçti**
- Python `compileall`: başarılı
- Kullanılmayan `_MetricPill` ve Kariyer `_SectionCard.trailing` kalıpları kaynakta bulunmuyor
- Veritabanı şeması ve Alembic başı değişmedi

## Yerel kabul

```powershell
cd client
.\tool\bootstrap_client.ps1
```

`flutter analyze` ve `flutter test` sıfır hata ile tamamlandığında rev1 kapanmış sayılır.
