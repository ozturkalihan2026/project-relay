# Project Relay v0.8.19 rev1 test raporu

## Düzeltme
İstatistikler ekranındaki `_LeaderboardCard`, kendisine verilen `season-leaderboard-card` anahtarını iç `Card` widget'ına tekrar aktarıyordu. Flutter finder bu nedenle aynı anahtarla iki widget buluyordu. İç `Card` üzerindeki `key: key` kaldırıldı; anahtar yalnız dış `_LeaderboardCard` üzerinde kaldı.

## Regresyon koruması
- `season_screen.dart` içinde `key: key` kalmaması kaynak sözleşmesiyle doğrulanır.
- Widget kabul testi `season-leaderboard-card` için `findsOneWidget` beklentisini korur.
- İstemci yapı numarası: `0.8.19+76`.
