# Project Relay v0.8.1 — Test Raporu

Tarih: 2 Ağustos 2026  
Paket: Project Relay v0.8.1  
İstemci: `0.8.1+43`  
API: `0.8.1`  
Savaş kuralları: `0.8`

## Paketleme ortamında tamamlanan kontroller

### Python derleme

```bash
python -m compileall -q relay_api relay_engine tests alembic
```

Sonuç: başarılı.

### Normal sunucu/API/migration/sözleşme testleri

```bash
RELAY_DATABASE_URL='sqlite+pysqlite:///:memory:' \
python -m pytest -q --ignore=tests/test_balance.py
```

Sonuç: **138/138 geçti.**

### Seçili denge regresyonları

```bash
RELAY_DATABASE_URL='sqlite+pysqlite:///:memory:' \
python -m pytest -q tests/test_balance.py \
  -k 'not no_server_strategy_is_unbeaten_across_seeded_matchups'
```

Sonuç: **5/5 geçti.** Çok uzun yenilmez-strateji matrisi bu hotfix kapsamında
yeniden çalıştırılmadı; savaş motoru ve denge dosyaları değiştirilmedi.

### Yapısal ve paket kontrolleri

- YAML ayrıştırması: `docker-compose.yml`, `client/pubspec.yaml`
- Alembic tek başı: `20260801_0009`
- Widget hotfix farkı: yalnız hedef test akışında görünürlük/finder koruması
- Arşivde geçici derleme, önbellek ve sanal ortam dosyaları bulunmaması
- ZIP bütünlük testi ve SHA-256 üretimi

## Ortam sınırı

Paketleme ortamında Flutter/Dart SDK bulunmadığından gerçek `flutter analyze` ve
`flutter test` çalıştırılamadı. Son Windows kabulü:

```powershell
cd client
.\tool\bootstrap_client.ps1
```

Bu nedenle sunucu paketi ve kaynak sözleşmeleri doğrulanmıştır; Flutter kabulü
yerel makinede sıfır hata çıktısı alındığında tamamen kapanacaktır.
