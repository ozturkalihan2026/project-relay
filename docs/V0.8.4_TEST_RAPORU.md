# Project Relay v0.8.4 — Test Raporu

**API:** `0.8.4`  
**Flutter istemci:** `0.8.4+50`  
**Savaş kuralları:** `0.8`  
**Alembic başı:** `20260801_0009`

## Doğrulanan kapsam

- Profil Genel ve Arkadaşlar sekmelerinin ayrılması
- Profil içindeki günlük görev ve başarım ödül akışlarının korunması
- Klan ekranının yalnız özet, üyeler, etkinlik ve ayarlara indirgenmesi
- Ortak Profil/Ayarlar/Nasıl Oynanır üst erişimi
- Kariyer ekranında doğrudan modül paleti ve düzenlenebilir devre kartı
- Oyuncu ve koşu rakibi devrelerinin aynı hazırlık alanında gösterilmesi
- Koşu ve savaş öncesinde devrenin doğrulanıp kaydedilmesi
- Kariyer içindeki yinelenen görev ve başarım alanlarının kaldırılması
- Komutan Sistemi'nin yalnız özellik havuzunda korunması

## Python doğrulaması

SQLite bellek veritabanıyla uzun denge matrisi hariç bütün normal paket:

```bash
RELAY_DATABASE_URL='sqlite+pysqlite:///:memory:' \
python -m pytest -q --ignore=tests/test_balance.py
```

Sonuç: **140/140 test geçti.**

Dağılım:

- `test_alpha.py`: 2
- `test_api.py`: 21
- `test_auth.py`: 3
- `test_career.py`: 8
- `test_client_contract.py`: 36
- `test_collection.py`: 6
- `test_competitive.py`: 6
- `test_engine.py`: 27
- `test_migrations.py`: 2
- `test_online.py`: 11
- `test_progression.py`: 9
- `test_season.py`: 4
- `test_social.py`: 4
- `test_social_api.py`: 1

## Seçili denge regresyonları

```bash
python -m pytest -q tests/test_balance.py \
  -k 'not no_server_strategy_is_unbeaten_across_seeded_matchups'
```

Sonuç: **5/5 geçti.**

Bu sürüm savaş motorunu veya denge katsayılarını değiştirmez. Çok uzun
`no_server_strategy_is_unbeaten_across_seeded_matchups` matrisi paketleme
süresinde yeniden çalıştırılmamıştır.

## Kaynak doğrulamaları

- Python `compileall`: başarılı
- 49 Dart kaynak/test dosyasında yorum ve string farkındalıklı ayraç taraması:
  başarılı
- Bilinen v0.8.3 null-aware ve klan ad çakışması kalıpları: bulunmadı
- API/istemci sürüm metinleri: `0.8.4` / `0.8.4+50`
- PostgreSQL şeması ve Alembic başı: değişmedi

## Flutter ortam sınırı

Paketleme ortamında Flutter/Dart SDK bulunmadığı için gerçek `flutter analyze`
ve `flutter test` burada çalıştırılamadı. Son yerel kabul komutu:

```powershell
cd client
.\tool\bootstrap_client.ps1
```

Paket v0.8.4 olarak ancak bu komut yerel Flutter ortamında sıfır hata ile
bittiğinde tamamen kapanmış sayılmalıdır.

## Paket bütünlüğü

- Temiz kaynak paketi: **225 dosya**
- `.venv`, `.dart_tool`, `build`, `.pytest_cache`, `__pycache__` ve derlenmiş
  Python dosyaları arşive alınmadı.
- ZIP bütünlük denetimi geçti.
- Arşiv ayrı klasöre yeniden açıldı.
- Yeniden açılan proje üzerinde 140 normal test ve 5 seçili denge testi tekrar
  geçti.

## rev2 widget test kabul düzeltmesi

- Profil geri düğmesi görünürlük animasyonu tamamlandıktan sonra tıklanır.
- Profil yatay bölüm yardımcısı yalnız seçilen sekmenin `Scrollable` atasını bulur.
- Ürün davranışı değişmez; gerçek `flutter test` sonucu yerel ortamda yeniden
  doğrulanmalıdır.
