# Project Relay v0.8.5 rev2 — Test Raporu

**Sunucu API:** `0.8.4`  
**Flutter istemci:** `0.8.5+53`  
**Savaş kuralları:** `0.8`  
**Alembic başı:** `20260801_0009`

## Düzeltilen kapsam

- Çevrimiçi Savaş widget testi `ÇEVRİMİÇİ SAVAŞ • v0.8.5` başlığını bekler.
- Antrenman widget testi `ANTRENMAN • v0.8.5` başlığını bekler.
- Eski `v0.8.4` beklentilerinin yeniden eklenmesi kaynak sözleşmesiyle engellenir.
- Savaş modlarının ayrı devre durumları, Kariyer kart eşitliği ve mağaza sekmeleri korunur.

## Python ve sözleşme testleri

```bash
RELAY_DATABASE_URL='sqlite+pysqlite:///:memory:' \
python -m pytest -q --ignore=tests/test_balance.py
```

Sonuç: **140/140 test geçti.**

## Seçili denge regresyonları

```bash
RELAY_DATABASE_URL='sqlite+pysqlite:///:memory:' \
python -m pytest -q tests/test_balance.py \
  -k 'not no_server_strategy_is_unbeaten_across_seeded_matchups'
```

Sonuç: **5/5 geçti.**

Uzun `no_server_strategy_is_unbeaten_across_seeded_matchups` matrisi bu test-only
hotfix paketlemesinde yeniden çalıştırılmadı. Savaş motoru ve denge katsayıları
değişmedi.

## Flutter yerel kabulü

Paketleme ortamında Flutter SDK bulunmadığından son gerçek kabul kullanıcının
Windows Flutter ortamında yapılır:

```powershell
cd client
.\tool\bootstrap_client.ps1
```

## Paket bütünlüğü

Paket `.venv`, `.dart_tool`, `build`, `.pytest_cache`, `__pycache__` ve derlenmiş
Python dosyalarını içermez. ZIP bütünlük kontrolü ve yeniden açılmış paket
regresyonları uygulanır.
