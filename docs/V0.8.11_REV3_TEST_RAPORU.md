# Project Relay v0.8.11 rev3 Test Raporu

## Hotfix
- `module_palette_layout_test.dart` artık gerçek drag gesture zamanlamasına bağımlı değildir.
- `ModulePaletteReturnBanner` üretim bileşeni 162 px genişlikte doğrudan render edilir ve overflow kontrolü yapılır.
- Karttan palete geri bırakma davranışı ayrı `BoardController` regresyon testleriyle korunur.

## Doğrulamalar
- İstemci kaynak sözleşmeleri: 41/41 geçti.
- Savaş motoru + denge testleri: 33/33 geçti.
- Flutter SDK bu paketleme ortamında bulunmadığı için gerçek `flutter analyze` ve `flutter test` yerel kabul adımında çalıştırılmalıdır.
