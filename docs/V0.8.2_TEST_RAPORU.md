# Project Relay v0.8.2 — Test Raporu

Tarih: 2 Ağustos 2026  
Paket: Project Relay v0.8.2  
İstemci: `0.8.2+44`  
API: `0.8.2`  
Savaş kuralları: `0.8`  
Alembic başı: `20260801_0009`

## Doğrulanan kapsam

- Sosyal Merkez'in Profil, Arkadaşlar ve Klan bölümlerine ayrılması
- arkadaş/gelen istek/giden istek/klan özet kartı
- giden arkadaşlık isteklerinin görünür olması
- boş oyuncu arama durumu ve oyuncu profil alt sayfası
- arkadaş silme, klana katılma, ayrılma ve klan kapatma onayları
- klan kapasitesi ve lider ayrılma kısıtı görünümü
- herkese açık metinlerde kişisel bilgi paylaşmama uyarıları
- Alfa Geri Bildirimi yönlendirmesi
- Komutan Sistemi ve Oyuncu Tarzı Profili özellik havuzu kaydı
- v0.8.1 Flutter gezinme test düzeltmesinin korunması
- v0.8.0 sosyal API, v0.7.0 sezon/alfa ve önceki savaş sistemlerinin gerilememesi

## Normal Python paketi

Komut:

```bash
RELAY_DATABASE_URL='sqlite+pysqlite:///:memory:' \
python -m pytest -q --ignore=tests/test_balance.py
```

Sonuç: **139/139 test geçti.**

Bu paket aşağıdaki alanları kapsar:

- savaş motoru ve replay
- FastAPI/OpenAPI sözleşmeleri
- JWT misafir oturumu
- PostgreSQL/Alembic sözleşmeleri
- asenkron PvP
- derece, lig ve maç geçmişi
- XP, görev, başarım ve Kariyer Koşusu
- koleksiyon, kozmetik ekonomi ve kontrollü kit
- sezon ve kapalı alfa korumaları
- sosyal profil, arkadaşlık ve klan servisi
- Flutter kaynak sözleşmeleri ve v0.8.2 sosyal arayüz kabul işaretleri

## Seçili denge regresyonları

Komut:

```bash
python -m pytest -q tests/test_balance.py \
  -k 'not no_server_strategy_is_unbeaten_across_seeded_matchups'
```

Sonuç: **5/5 test geçti.**

Uzun `no_server_strategy_is_unbeaten_across_seeded_matchups` matrisi bu
paketleme çalışmasında yeniden tamamlanmadı. v0.8.2 savaş motoruna, modül
kataloğuna veya denge katsayılarına dokunmaz.

## Kaynak ve yapı doğrulamaları

- `python -m compileall -q relay_api relay_engine tests`: başarılı
- 47 Dart kaynak/test dosyasında string ve yorum farkındalıklı ayraç taraması:
  **47/47 geçti**
- `pubspec.yaml`: başarıyla ayrıştırıldı
- `docker-compose.yml`: başarıyla ayrıştırıldı
- `alembic heads`: `20260801_0009 (head)`

## Flutter ortam sınırı

Paketleme ortamında Flutter ve Dart SDK komutları kurulu değildir. Bu nedenle
gerçek `flutter analyze` ve `flutter test` burada çalıştırılamadı. Son yerel kabul:

```powershell
cd client
.\tool\bootstrap_client.ps1
```

Bu komut `flutter pub get`, `flutter analyze` ve `flutter test` adımlarını
çalıştırır. v0.8.2 tamamlanma kaydı, yerel Flutter çıktısında sıfır hata ile
kapatılmalıdır.

## Değişmeyen alanlar

- yeni Alembic migration yoktur
- PostgreSQL tablo şeması değişmez
- savaş kuralları `0.8` kalır
- modül değerleri ve bot düzenleri değişmez
- sosyal sistem savaş gücü sağlamaz
- raporlama, engelleme, klan savaşı, sohbet ve liderlik devri eklenmemiştir
